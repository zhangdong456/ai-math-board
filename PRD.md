# AI 动态知识演示白板 — 产品需求文档（PRD）

> 本文档是需求的唯一权威来源。后续任何新需求或修改，都必须先更新本文档再开发。

## 1. 产品定位

面向数学、物理、化学等学科的 **AI 知识可视化白板**（不是 AI 动画生成器）。

核心体验闭环：

- 看到一个知识点 → 框选 → AI 理解 → 自动生成**可交互**动态演示 → 在白板上讲解
- 输入知识点文字 → AI 生成 → 在白板中交互演示

优先级原则：**准确性 > 可解释性 > 动画效果**。

## 2. 已确认的关键决策

| 决策项 | 结论 |
|---|---|
| 产品形态 | 纯网页，响应式布局适配平板触屏；后续可用 Electron 套壳桌面端 |
| 技术栈 | React + Vite + TypeScript |
| 架构 | 纯前端：浏览器直连各厂商 LLM API，API Key 存 localStorage |
| AI 生成方式 | AI 只输出**结构化 JSON 知识描述**，由本地演示引擎渲染（幻觉可控） |
| 交付策略 | **核心闭环优先**：先交付文字生成 + 图片框选生成两大场景及配套窗口/画笔/模型配置，再迭代高级功能 |
| 默认引擎模板 | 尽可能多（在不牺牲核心闭环稳定性的前提下逐步扩充） |
| 白板状态 | localStorage 持久化（窗口、标注、图片、模型配置刷新后保留） |
| 内置模型厂商 | DeepSeek、Kimi（月之暗面）、GLM（智谱）、OpenCode Go、OpenCode Zen，均只需填 Key；另提供「自定义 OpenAI 兼容接口」可改 BaseURL |

## 3. 技术方案

### 3.1 技术栈与关键库

- React 18 + Vite + TypeScript
- 状态管理：Zustand（白板窗口、标注、模型配置等全局状态）
- 画布标注：原生 Canvas（自绘，覆盖在最上层）
- 演示渲染：SVG / Canvas 自绘（函数曲线、物理模型、分子模型均由结构化 JSON 驱动）
- 样式：CSS Modules 或 Tailwind（二选一，以简洁现代为准）
- 持久化：localStorage（Zustand persist）

### 3.2 整体架构

```
用户输入（文字 / 图片框选）
  ↓
AI 视觉识别 / 文本理解（多模态 LLM，输出结构化 JSON）
  ↓
知识验证层 Knowledge Validation Layer（本地规则校验）
  ↓
动态演示引擎（本地渲染器，JSON → 可交互组件）
  ↓
白板窗口展示（可拖参数、实时变化）
```

- AI **不直接生成** HTML/动画代码，只生成结构化知识描述（type、formula、parameters、range、effect、规律说明等）。
- 本地渲染器按 `type` 分发到对应演示组件（二次函数、三角函数、F=ma、化学反应……）。
- AI 输出未通过校验层或置信度不足时，**禁止强行生成**，提示用户（如"公式识别不清，请上传更清晰的图片"）。

### 3.3 目录结构（规划）

```
src/
  components/       # 通用 UI 组件
  board/            # 白板核心：窗口系统、画布、框选
  windows/          # 各类窗口（图片窗口、演示窗口外壳）
  engines/          # 动态演示引擎：各学科渲染器
  ai/               # LLM 接入：厂商适配、提示词、JSON 解析
  validation/       # 知识验证层
  store/            # Zustand 状态
  config/           # 模型厂商预设（BaseURL、模型列表、vision 标记）
```

## 4. 功能需求

### 4.1 页面布局（三栏）

- **左侧：AI 操作区** — 文本输入框（公式/知识点/演示要求）、学科选择（数学|物理|化学|其他）、「AI 生成动态演示」按钮、图片上传入口、默认引擎模板列表
- **中间：核心白板**（占宽最大）— 多窗口容器 + 自由标注层
- **右侧：工具区** — 画笔/标注工具、AI 生成结果说明（AI 理解、核心规律、验证状态）

### 4.2 白板窗口系统

所有窗口（图片窗口、演示窗口）统一能力：

- 拖动、缩放、调整大小（拖边缘）、最大化、最小化、关闭、调整层级（z-index）
- 多窗口同时存在
- **新窗口必须出现在当前可视区域内**；空间不足时放白板中央并置顶，严禁跑到视口外

### 4.3 场景一：文字生成（核心闭环 1）

1. 用户输入如 "y = ax² + bx + c，展示参数变化对函数图像的影响"
2. 点击「AI 生成」→ AI 返回结构化 JSON → 校验 → 渲染
3. 生成可交互动态演示：坐标系、曲线、a/b/c 参数滑块、顶点/对称轴实时变化

### 4.4 场景二：图片框选生成（核心闭环 2）

1. 上传图片（教材截图/公式/函数图像/黑板/PPT/网页截图等）→ 图片作为独立窗口显示在白板
2. 用户在图片窗口内框选目标区域
3. **框选框使用高对比度红色描边**，有明显选中状态（禁止浅色）
4. 点击「AI 动态演示」→ 裁剪区域图发给多模态 AI 识别
5. AI 完成：识别内容 → 判断知识点 → 理解变量参数 → 选择演示形式 → 输出结构化 JSON
6. 生成演示窗口，**出现在当前可视区域并置顶**

### 4.5 右侧画笔 / 标注工具

在白板**任意区域**（覆盖层，不限于某窗口内）标注：

- 自由画笔（可调粗细、颜色）
- 图形：正方形、矩形、圆形、椭圆、箭头
- 橡皮擦、撤销、重做、清空标注

### 4.6 AI 模型配置（独立页面/面板）

- 第一步选厂商（不让用户手输 API 地址）→ 自动填 BaseURL → 用户只填 API Key
- 内置厂商及预设：

| 厂商 | BaseURL | 说明 |
|---|---|---|
| DeepSeek | `https://api.deepseek.com/v1` | 无视觉模型，仅支持文字生成场景 |
| Kimi（月之暗面） | `https://api.moonshot.cn/v1` | 含视觉模型（moonshot-v1-*-vision-preview / kimi-latest） |
| GLM（智谱） | `https://open.bigmodel.cn/api/paas/v4` | 含视觉模型（glm-4v 系列） |
| OpenCode Go | `https://opencode.ai/zen/go/v1` | OpenAI 兼容 |
| OpenCode Zen | `https://opencode.ai/zen/v1` | OpenAI 兼容 |
| 自定义 | 用户填 BaseURL + Key + 模型名 | OpenAI 兼容接口 |

- 每个厂商旁提供「如何获取 API Key」说明（步骤式）
- 「获取模型列表」按钮：调 `GET {BaseURL}/models` 拉取该 Key 可用模型，点击选择；视觉能力按预设匹配 + 命名启发式（vision/-vl/4v/omni）标注
- API Key 密码框输入（••••），带显示/隐藏切换
- 「测试连接」按钮
- **模型筛选与标注**：明确标注 ✅ 支持图片识别 / ❌ 不支持；默认只推荐多模态模型；图片框选场景下若所选模型无视觉能力，明确拦截并提示
- **CORS 实测结论（2026-08）**：DeepSeek / Kimi / GLM 浏览器直连可用；**OpenCode Go / Zen 服务端无 CORS 支持，网页无法直连**，UI 已标注，需经 OpenAI 兼容中转代理后走「自定义」接入

### 4.7 默认动态演示引擎（无 API Key 可用）

未配置 Key 时产品必须可用。每个模板即一份手工校验过的结构化 JSON，走同一渲染管线。

**已实现模板清单（22 个，M1+M2）**：

- 数学（12）：一次函数、二次函数、三角函数（sin/cos/tan）、圆、矩形面积（单位网格）、三角形面积（等底等高演示）、平行四边形面积（割补）、圆面积与割圆术（内接正 n 边形逼近 π）、指数函数、导数几何意义（割线逼近切线）等
- 物理（6）：F=ma、匀变速直线运动（v-t 图阴影面积=位移）、欧姆定律（量程自适应）、平抛运动（速度分量分解）等
- 化学（4）：氢氧燃烧配平（原子守恒条形对比）、玻尔原子模型（Z 1~20 电子层）、分子 3D 球棍模型（纯 SVG 手写旋转投影，CH₄/H₂O/CO₂/NH₃ 由 KnowledgeJSON 可选字段 `variant` 选择）等

规划中的其余方向（数列、对数、抛物线、简谐振动/波、酸碱中和等）在后续迭代继续扩充。

### 4.8 知识准确性与幻觉控制

- 知识验证层：校验公式、单位、参数关系、学科规律（如 a>0 开口向上、|a| 越大开口越窄；F=ma 改质量不得错显速度变化；化学反应原子守恒）
- **已落地（M2）**：验证层重构为规则表驱动（按演示类型声明必需参数、正长度夹取/拒绝、取整、范围夹取、variant 回退等规则），并新增学科↔类型一致性检查（不一致时标记 ⚠ 待确认提示，不阻塞生成）
- AI 不确定时禁止编造，给出明确提示
- 每个生成的演示显示**知识验证状态**：✅ 已验证 / ⚠ 待确认（附原因）
- 每次生成同时显示「AI 理解」与「核心规律」文字说明

### 4.9 学科分类

顶部或左侧提供学科选择：数学 | 物理 | 化学 | 其他，作为提示词上下文提升生成准确度。

### 4.10 UI / 体验

- 风格：简洁、现代、科技感、教育感、专业；操作路径一目了然（上传→框选→生成 或 输入→生成）
- 滚动条：细、简洁、与 UI 统一，禁用浏览器默认粗滚动条；核心操作区保持在视口内，减少整页滚动
- 响应式：适配平板触屏（拖动、框选、画笔均需支持 touch 事件）
- **已落地（M3）**：左右侧栏可折叠（折叠后留竖排把手条，窄屏首屏自动折叠）；模板列表按当前学科过滤；窗口智能排列（一键平铺 / 级联）；窗口拖动缩放与画笔草稿做 rAF 节流；触屏交互热区与点按体验打磨

## 5. 持久化设计（localStorage）

- `modelConfig`：各厂商 Key（明文存本地，仅本机使用，需在 UI 注明）、当前选用厂商/模型
- `boardState`：窗口列表（类型、位置、尺寸、层级、演示 JSON、图片 dataURL）、标注笔画
- 图片以 dataURL 存储，超大图片需压缩后存储（控制 localStorage 容量）

## 6. 里程碑

- **M1 核心闭环**：三栏布局 + 窗口系统 + 文字生成（默认引擎，含二次函数完整交互）+ 图片上传/红色框选 + AI 框选生成 + 模型配置（含测试连接、vision 过滤）+ 画笔标注 + localStorage 持久化
- **M2 扩充**（已完成）：更多默认模板（数理化全覆盖）、知识验证层规则库完善、验证状态展示
  - 实际交付：新增 12 种演示渲染器（矩形/三角形/平行四边形/圆面积与割圆术、指数函数、导数几何意义、匀变速直线运动、欧姆定律、平抛运动、玻尔原子模型、氢氧燃烧配平、分子 3D 球棍模型），渲染类型总数 17；默认模板 6 → 22（数学 12 / 物理 6 / 化学 4）；验证层重构为规则表驱动并新增学科↔类型一致性检查
- **M3 体验**（已完成）：平板触屏打磨、性能优化、窗口布局智能排列
  - 实际交付：侧栏可折叠（窄屏自动折叠）+ 学科过滤模板列表；窗口一键平铺/级联排列；窗口拖动缩放 rAF 节流、演示窗口 React.memo、画笔草稿 useRef + rAF 合并重绘；触屏热区与点按体验打磨（touch-action、加大的手柄/按钮/滑块、输入框 14px 防 iOS 缩放）

## 7. 已知限制与风险

- 纯前端直连 LLM 受各厂商 CORS 策略限制。实测：DeepSeek / Kimi / GLM 可直连；**OpenCode Go / Zen 预检 404 且无 CORS 头，浏览器无法直连**，需中转代理后走「自定义」接入
- API Key 存 localStorage 有 XSS 泄露风险，仅限个人本机使用场景，UI 需明确提示
- localStorage 容量约 5MB，多张大图持久化需压缩或降级为仅存窗口布局
- AI 返回 JSON 需严格 schema 校验 + 失败重试/兜底提示
